/** Design option 4 — client preview at `/guest-4`. */
export function Guest4Page() {
  return (
    <main className="flex min-h-svh items-center justify-center bg-paper-50">
      <p className="text-sm text-ink-500">Guest page option 4 — design placeholder</p>
    </main>
  );
}
